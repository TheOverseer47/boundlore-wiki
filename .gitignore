# Local VS Code workspace settings
.vscode/

# Staging secrets (P5-STAGING.1) — template is .env.staging.example
.env.staging

# Legacy DB credentials (P5-STAGING.5B) — never commit
.env.legacy

# Local staging backups (P5-STAGING.3) — never commit dumps
backups/

# P5-E.9F.6 raw legacy export (contains BLMETA) — never commit
qa/fixtures/.real-content-export-raw.json
qa/fixtures/real-content-export/
