# BoundLore staging environment — template only (P5-STAGING.1)
# Copy to .env.staging locally and fill with real values.
# NEVER commit .env.staging. NEVER commit service_role or DB passwords.

SUPABASE_STAGING_PROJECT_REF=
SUPABASE_STAGING_URL=
SUPABASE_STAGING_ANON_KEY=
SUPABASE_STAGING_DB_HOST=
SUPABASE_STAGING_DB_URL=
SUPABASE_STAGING_CONFIRM_ISOLATED=false
